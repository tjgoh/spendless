services:
  db:
    image: postgres:18
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: spendless
      POSTGRES_PASSWORD: password
      POSTGRES_USER: spendless
    volumes:
      - pgdata:/var/lib/postgresql

volumes:
  pgdata:
